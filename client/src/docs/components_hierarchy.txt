Components Hierarchy

App
│
├── NavigationMenu
│
├── Shared UI Components
│   └── QuestionReviewCard (Reusable correct/incorrect answer view card)
│
├── Auth
│   ├── Login
│   └── Register
│
├── Teacher Pages
│   ├── TeacherDashboard
│   ├── CreateExam
│   ├── TeacherStudentResults
│   │   └── SubmissionReviewModal (Detailed Review & Override Modal)
│   └── LiveMonitor (Live Student Activity Stream)
│
├── Student Pages
│   ├── StudentPortal (Exam Taker View)
│   └── StudentResults (Grades, Comments & Queries View)
│       └── StudentSubmissionReviewModal (Student Answer Review Modal)
│
├── API Services
│   ├── apiClient (Centralized authorization headers extractor)
│   ├── authService
│   ├── examService
│   ├── scoreService
│   ├── feedbackService
│   └── monitorService
│
└── Reusable Utilities
    ├── ConfigService
    ├── LoggerService
    ├── StorageService
    └── NotifyService

Explanation:

App is the root component:
- Handles user authentication state, loading results/feedbacks, and responsive routing.

NavigationMenu:
- Renders the header options (Dashboard, Portal, Results, Theme toggler, Logout) according to the user's role. Hidden completely when student is actively taking an exam.

QuestionReviewCard:
- A reusable presentation card to display individual question correctness, student choices, reference answers, and point values.

apiClient:
- Centralized client-side API helper that generates standard authorization headers containing stateless JWT tokens.

Login / Register:
- Authentication forms with input validations.

TeacherDashboard:
- Lists all draft and published exams with stats and access to ExamManagement.

CreateExam:
- Form to configure and save new exam headers.

TeacherStudentResults:
- Displays submitted scores, class averages, grade distributions, and bulk tools.
- Spawns SubmissionReviewModal to allow manual grading and written feedback.

StudentPortal:
- Enables starting assessments, navigating questions, and submitting answers.

StudentResults:
- Displays published grades, teacher feedback, and lets students submit custom queries.
- Spawns StudentSubmissionReviewModal to let students review their answers and see correct options.

LiveMonitor:
- Displays active student test-taking sessions, elapsed duration, and status heartbeats in real-time.